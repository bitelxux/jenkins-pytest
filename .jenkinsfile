node(){

    stage('setup'){
        //sh "mkdir -p tests-results"
    }

    stage('checkout'){
        checkout scm
    }

    stage("tests"){
        sh "pytest tests/test.py --tb=line --junitxml=tests-results/results.xml"
    }

    stage("publish"){
       junit allowEmptyResults: true, testResults: 'tests-results/results.xml'
    }
}
