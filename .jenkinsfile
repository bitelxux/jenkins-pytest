node(){

    stage('checkout'){
        checkout scm
    }

    stage("tests"){
        sh "pytest tests/test.py --tb=line --junitxml=test-results/results.xml"
    }

    stage("publish"){
       junit allowEmptyResults: true, testResults: 'tests-results/results.xml'
    }
}
