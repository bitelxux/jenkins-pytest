node(){

    stage('checkout'){
        checkout scm
    }

    stage("tests"){
        sh "pytest tests/test.py --tb=line --junitxml=results.xml"
    }

    stage("publish"){
       junit 'results.txt'
    }
}
